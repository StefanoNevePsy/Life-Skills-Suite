// Termometro delle emozioni: per ogni emozione, livelli di intensità
// crescente con immagini illustrative predefinite (memorizzate come asset statici).
// L'utente può comunque personalizzarle o caricarne di proprie (salvate in IndexedDB).

import defaultData from './thermometerDataDefault.json';

export const EMOTION_THERMOMETER_DEFAULT = defaultData;
